apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "siphon.fullname" . }}-config
  labels:
    {{- include "siphon.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
