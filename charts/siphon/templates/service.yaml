apiVersion: v1
kind: Service
metadata:
  name: {{ include "siphon.fullname" . }}
  labels:
    {{- include "siphon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "siphon.selectorLabels" . | nindent 4 }}
